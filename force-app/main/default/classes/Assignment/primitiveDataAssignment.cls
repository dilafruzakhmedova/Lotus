public class primitiveDataAssignment {
    public void dataId() {

        ID ContactId = '00BDo0000022mgEMAQ';
        string ContactData = ' Is this ID belongs to Accounts? ';
        boolean isContactId = true;

        system.debug('Contact ID: ' + contactId + contactData + isContactId);

    }
}
